# Example /etc/default/tfnet-sync -- copy to /etc/default/tfnet-sync and edit.
#
# These flags are passed to `tfnet sync` by the tfnet-sync.service unit.
# See `tfnet sync -h` for the full flag list.

TFNET_SYNC_OPTS="-repo /var/lib/tfnet/repo -self alice -hooks-dir /etc/tfnet/hooks.d"
