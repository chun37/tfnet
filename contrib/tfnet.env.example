# Example /etc/default/tfnet -- copy to /etc/default/tfnet and edit.
#
# These flags are passed to both 'tfnet start' and 'tfnet stop' by the
# systemd unit tfnet@.service.

TFNET_OPTS="-ledger /var/lib/tfnet/ledger -wg-key /etc/tfnet/wg.json -asn 65010"
